import { Typography } from "@mui/material";

export default function CheckoutPage(){
    return(
        <Typography>
            only logged users should be able to use this!
        </Typography>
    )
}